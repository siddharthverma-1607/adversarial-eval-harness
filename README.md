# adversarial-eval-harness
System that runs a set of jailbreak attempts against a language model, judges each outcome, and records the results. When a system prompt or the model version is changed and the battery is run again, the tool surfaces how the behavior shifted between the two runs.
